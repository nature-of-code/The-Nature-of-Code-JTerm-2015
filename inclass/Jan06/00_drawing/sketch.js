
function setup() {
  createCanvas(600, 400);
  
  // This is the background
  background(100);

  // This is the stroke
  stroke(255);
  fill(127);

  // ellipse(50, 50, 100, 100);
  // angleMode(DEGREES);
  // arc(50, 200, 100, 100, 27, 196);
  
  // This is using vertex
  // beginShape();
  // vertex(100,100);
  // vertex(120,300);
  // vertex(50,75);
  // endShape(CLOSE);



}

